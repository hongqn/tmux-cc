{
  "name": "tmux-cc",
  "version": "0.1.0",
  "private": true,
  "description": "OpenClaw provider plugin that delegates inference to Claude Code CLI running in tmux sessions",
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.1.2"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  },
  "dependencies": {
    "@mariozechner/pi-ai": "^0.63.1",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.3.6"
  }
}
